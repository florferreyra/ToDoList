Future functionalities

-Add a date picker to select dates.
-Use Celery so that when a task is close to expiring notify by mail.
-Add a checkbox to change the status of a task.
-Add a filter, filter by status (success, pending, all).
-Add a search engine.
